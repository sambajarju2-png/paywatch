/**
 * @deprecated The tier + feature config now lives in @paywatch/config — the single
 * source of truth shared by b2b and admin. This file re-exports it so existing
 * imports keep working. In new code import from "@paywatch/config" directly.
 */
export * from "@paywatch/config";
